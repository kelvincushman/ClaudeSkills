# Atopile Build Examples

## Standard Build Workflow
```bash
# 1. Compile and update layout
ato build

# 2. Open KiCAD to perform routing
# (KiCAD opens automatically if single target)
```

## Debugging Build Failures
```bash
# Use verbose mode to see solver details
ato -v build
```
